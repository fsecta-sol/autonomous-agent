// The execution-trace model is shared with the backend (which replays a
// persisted event log). Re-exported here so existing `@/lib/execution` imports
// keep working unchanged.
export * from "@dashboard/shared/execution";
